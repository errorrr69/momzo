{
  "name": "momzo-corpus-seed",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Idempotent content-corpus ingest: parse -> why_it_matters (Gemini) -> chunk -> embed (Gemini) -> seed content_cards + content_embeddings.",
  "scripts": {
    "seed": "node build_corpus.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
